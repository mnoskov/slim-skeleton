<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}{% endblock %}</title>
        <link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/css?family=Roboto:300,700&amp;subset=cyrillic,cyrillic-ext">
        <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.0.0-alpha.4/css/bootstrap.min.css">
        <link rel="stylesheet" type="text/css" href="/css/style.css?v{{ app.version }}">
    </head>

    <body>
        <div class="container">
            {% if flash.errors %}
                <div class="global-error alert alert-danger">
                    {% for msg in flash.errors %}
                        {{ msg }}<br>
                    {% endfor %}
                </div>
            {% endif %}

            {% if flash.success %}
                <div class="global-success alert alert-success">
                    {% for msg in flash.success %}
                        {{ msg }}<br>
                    {% endfor %}
                </div>
            {% endif %}

            {% block body %}{% endblock %}
        </div>

        <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
        <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/tether/1.4.0/js/tether.min.js"></script>
        <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.0.0-alpha.4/js/bootstrap.min.js"></script>
        <script type="text/javascript" src="/js/common.js?v{{ app.version }}"></script>
    </body>
</html>
